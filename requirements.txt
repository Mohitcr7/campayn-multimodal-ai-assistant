# ── Gemini LLM (intent parser + generation) ──────────────────────────────
google-generativeai==0.7.2        # old SDK — intent_parser Path B fallback
google-genai==1.2.0               # new SDK (from google import genai) — graph/rag/vision
                                  # pinned to 1.2.0: later versions require httpx>=0.28,
                                  # which breaks anthropic/langsmith (proxies kwarg)

# ── Anthropic Claude (automatic fallback if Gemini fails) ────────────────
anthropic==0.39.0

# ── LangChain / LangGraph / LangSmith stack ──────────────────────────────
langchain==0.2.16
langchain-google-genai==1.0.10
langchain-openai==0.1.25          # embeddings only
langchain-community==0.2.16
langgraph==0.2.28
langsmith==0.1.117

# ── Vector store ─────────────────────────────────────────────────────────
chromadb==0.5.15
faiss-cpu==1.8.0

# ── Embeddings ───────────────────────────────────────────────────────────
openai==1.51.0                    # text-embedding-3-small
tiktoken==0.8.0

# ── Data processing ──────────────────────────────────────────────────────
pandas==2.2.3
numpy==1.26.4

# ── API ──────────────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.31.0
python-multipart==0.0.12
pydantic==2.9.2
pydantic-settings==2.5.2

# ── Conversation memory ──────────────────────────────────────────────────
redis==5.1.0

# ── Document loaders ─────────────────────────────────────────────────────
python-docx==1.1.2
pypdf==4.3.1

# ── UI ───────────────────────────────────────────────────────────────────
streamlit==1.38.0

# ── Observability ────────────────────────────────────────────────────────
loguru==0.7.2

# ── Testing ──────────────────────────────────────────────────────────────
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2

# ── Dev ──────────────────────────────────────────────────────────────────
python-dotenv==1.0.1